import { MeshNormalMaterial } from 'three';

export default function Experience() {
    return (
        <mesh>
            <boxBufferGeometry />
            <meshNormalMaterial />
        </mesh>
    );
}
